# マッチング機能 タスク一覧

## 概要
エンジニア懇親会で、GitHubリポジトリ言語統計に基づいてリアルタイムにマッチングする機能。

---

## 要件

- エンジニアの懇親会でのマッチングを想定
- 1日に複数回マッチングする
- 一度マッチングしたユーザーとは再マッチングしない
- シンクロ度（類似度）によって、話す時間を変動させる
- 話終わったら、free 状態に戻る

---

## 完了済み

- [x] GitHub OAuth認証 (`/github/callback`)
- [x] GithubToken モデル（アクセストークン保存）
- [x] Language モデル（言語名 + Devicon アイコン）
- [x] UserRepository モデル（リポジトリ情報保存）
- [x] 言語統計API (`GET /api/user/github/languages/`)
- [x] 24時間キャッシュ（GitHub API呼び出し削減）

---

## 未着手

### 1. データモデル追加

#### UserStatus（ユーザー状態）
```python
class UserStatus(models.Model):
    user = OneToOneField("User")
    status = CharField(choices=["free", "matching", "talking"])
    current_match = ForeignKey("User", null=True)  # 現在の相手
    updated_at = DateTimeField(auto_now=True)
```

#### MatchHistory（マッチング履歴）
```python
class MatchHistory(models.Model):
    user1 = ForeignKey("User")
    user2 = ForeignKey("User")
    similarity_score = FloatField()  # シンクロ度
    talk_duration = IntegerField()   # 話す時間（秒）
    matched_at = DateTimeField()
    ended_at = DateTimeField(null=True)
    event_date = DateField()  # その日のイベント識別用
```

### 2. マッチングロジック

#### 類似度計算 (`main/services/matching.py`)
- [ ] ユーザーの言語ベクトル生成
- [ ] コサイン類似度計算
- [ ] シンクロ度 → 話す時間の変換ロジック
  ```python
  def get_talk_duration(similarity_score):
      # 例: 0-100% → 1-5分
      base_minutes = 1
      max_minutes = 5
      return base_minutes + (similarity_score * (max_minutes - base_minutes))
  ```

#### マッチング実行
- [ ] free状態のユーザーを取得
- [ ] 当日マッチ済みユーザーを除外
- [ ] 最も類似度の高いペアを選出

### 3. API エンドポイント

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/api/matching/status/` | 自分の状態を取得 |
| POST | `/api/matching/join/` | free状態で参加 |
| POST | `/api/matching/leave/` | 退出（free解除） |
| GET | `/api/matching/find/` | マッチング相手を探す |
| POST | `/api/matching/end/` | 会話終了、free に戻る |
| GET | `/api/matching/history/` | 今日のマッチング履歴 |

### 4. Serializer
- [ ] `UserStatusSerializer`
- [ ] `MatchResultSerializer`（相手情報 + シンクロ度 + 話す時間）
- [ ] `MatchHistorySerializer`

### 5. フロー

```
[参加] POST /api/matching/join/
   ↓
[状態: free]
   ↓
[探す] GET /api/matching/find/
   ↓
[マッチ成立] → 状態: talking, 相手情報 + 話す時間を返す
   ↓
[話す]（クライアント側でタイマー表示）
   ↓
[終了] POST /api/matching/end/
   ↓
[状態: free] → 次のマッチへ
```

### 6. 仕様書更新
- [ ] マッチングAPIの仕様を `仕様書.md` に追記

---

## 決定事項メモ

| 項目 | 決定内容 | 備考 |
|------|----------|------|
| 類似度計算 | コサイン類似度 | シンプルで直感的 |
| 話す時間 | 1〜5分（シンクロ度に比例） | 調整可能 |
| マッチング除外 | 当日マッチ済みユーザー | event_date で判定 |
| 状態管理 | free / matching / talking | |

---

## 優先度

**Phase 1: MVP**
1. UserStatus, MatchHistory モデル
2. マッチングロジック（類似度計算）
3. API エンドポイント（join, find, end, status）

**Phase 2: 改善**
4. 履歴API
5. 重み付け（時系列、スター数）
6. WebSocket対応（リアルタイム通知）
