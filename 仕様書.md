# Spajam2025 仕様書

## 1. プロジェクト概要

タイムカプセル共有プラットフォーム「Spajam2025」のバックエンドアプリケーション。
ユーザーが「カプセル」を作成し、他のユーザーを招待してリアルタイムで共同作業ができるソーシャルプラットフォーム。

### 主な機能
- タイムカプセルの作成・管理
- マルチメディアコンテンツの共有
- ユーザー招待・参加機能
- WebSocketによるリアルタイム同期
- 位置情報連携

---

## 2. 技術スタック

### バックエンド
| 技術 | バージョン | 用途 |
|------|-----------|------|
| Django | 3.1.6 | Webフレームワーク |
| Django REST Framework | 3.12.2 | REST API |
| Django Channels | 3.0.3 | WebSocket対応 |
| Python | 3.10+ | 言語 |

### データベース・キャッシュ
| 技術 | バージョン | 用途 |
|------|-----------|------|
| SQLite | - | 開発環境 |
| MySQL | 5.7 | 本番環境 |
| Redis | latest | メッセージブローカー・キャッシュ |

### 認証
- djangorestframework-jwt 1.11.0 (JWT認証)

### ストレージ・外部サービス
- AWS S3 (ファイルストレージ)
- AWS SES (メール送信)
- Firebase (リアルタイム機能)
- OpenAI API
- Google Cloud Translation
- Stripe (決済)

---

## 3. ディレクトリ構成

```
spajam2025/
├── config/                 # Django設定
│   ├── settings/          # 環境別設定
│   │   ├── base.py       # 基本設定
│   │   ├── dev.py        # 開発環境
│   │   └── prd.py        # 本番環境
│   ├── routing.py         # WebSocketルーティング
│   ├── urls.py            # URLルーティング
│   ├── asgi.py            # ASGIエントリポイント
│   └── wsgi.py            # WSGIエントリポイント
├── main/                   # コアAPIアプリケーション
│   ├── models/            # データモデル
│   ├── views/             # APIビュー
│   ├── serializers/       # シリアライザー
│   └── permissions.py     # 権限管理
├── room/                   # WebSocketコンシューマー
│   └── consumers/
├── tweet/                  # ツイート機能（未統合）
├── tag/                    # タグ機能（未統合）
├── web/                    # Djangoテンプレートビュー（部分実装）
├── shells/                 # デプロイスクリプト
├── schema/                 # APIスキーマドキュメント
├── static/                 # 静的ファイル
├── pyproject.toml          # 依存関係（Poetry）
├── docker-compose.yaml     # 開発用Docker
├── docker-compose.prd.yaml # 本番用Docker
└── Makefile               # 開発コマンド
```

---

## 4. データモデル

### 4.1 User（ユーザー）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| id | UUID | 主キー |
| email | EmailField | メールアドレス（ユニーク） |
| password | CharField | パスワード |
| username | CharField | ユーザー名 |
| icon | ImageField | アイコン画像 |
| device_uuid | CharField | デバイスUUID |
| email_confirmed | BooleanField | メール確認済みフラグ |
| deleted_at | DateTimeField | 削除日時（ソフトデリート） |
| created_at | DateTimeField | 作成日時 |
| updated_at | DateTimeField | 更新日時 |

### 4.2 Capsule（カプセル）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| id | UUID | 主キー |
| owner | ForeignKey(User) | オーナー |
| members | ManyToMany(User) | メンバー |
| title | CharField | タイトル |
| description | TextField | 説明 |
| locked | BooleanField | ロック済み（確定）フラグ |
| creating | BooleanField | 作成中フラグ |
| images | ManyToMany(Image) | 関連画像 |
| created_at | DateTimeField | 作成日時 |
| updated_at | DateTimeField | 更新日時 |

### 4.3 Image（画像）

| フィールド | 型 | 説明 |
|-----------|-----|------|
| id | UUID | 主キー |
| user | ForeignKey(User) | アップロードユーザー |
| image | ImageField | 画像ファイル |
| created_at | DateTimeField | 作成日時 |

### 4.4 認証関連モデル

#### AuthDigit（認証コード）
- 6桁の認証コード
- メール確認用
- 有効期限管理

#### PasswordResetToken（パスワードリセットトークン）
- セキュアなトークン
- パスワードリセット用
- 有効期限管理

#### LoginInvalid（ログイン失敗）
- ログイン失敗の追跡
- レート制限用

### 4.5 ER図

```
User 1───N Capsule (owner)
User M───N Capsule (members)
User 1───N Image
Capsule M───N Image
User 1───N AuthDigit
User 1───N PasswordResetToken
```

---

## 5. API エンドポイント

### 5.1 認証

| メソッド | パス | 説明 |
|---------|------|------|
| POST | /api/auth/user/ | ログイン（メール/パスワード） |
| POST | /api/auth/uuid/ | ログイン（UUID） |
| POST | /api/auth/refresh/ | JWTトークン更新 |
| POST | /api/auth/verify/ | JWTトークン検証 |

### 5.2 登録

| メソッド | パス | 説明 |
|---------|------|------|
| POST | /api/register/user/ | ユーザー登録 |
| POST | /api/register/uuid/ | UUID登録 |
| POST | /api/register/dummy/ | ダミーユーザー作成（開発用） |

### 5.3 ユーザー

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/user/ | 現在のユーザー取得 |
| PUT | /api/user/ | 現在のユーザー更新 |
| DELETE | /api/user/ | アカウント削除 |
| PUT | /api/user/password/ | パスワード変更 |
| GET | /api/users/ | ユーザー一覧 |
| GET | /api/users/{id}/ | ユーザー詳細 |

### 5.4 カプセル

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/capsules/ | カプセル一覧 |
| POST | /api/capsules/ | カプセル作成 |
| GET | /api/capsules/{id}/ | カプセル詳細 |
| PUT | /api/capsules/{id}/ | カプセル更新 |
| DELETE | /api/capsules/{id}/ | カプセル削除 |
| POST | /api/capsules/{id}/lock/ | カプセルをロック |
| POST | /api/capsules/{id}/join/ | カプセルに参加 |

### 5.5 画像（カプセル内）

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/capsules/{id}/images/ | 画像一覧 |
| POST | /api/capsules/{id}/images/ | 画像アップロード |
| GET | /api/capsules/{id}/images/{image_id}/ | 画像詳細 |
| DELETE | /api/capsules/{id}/images/{image_id}/ | 画像削除 |

### 5.6 その他

| メソッド | パス | 説明 |
|---------|------|------|
| GET | /api/status/ | システムステータス |
| GET | /schema/ | Swagger UI |
| GET | /schema.yaml/ | OpenAPI仕様（YAML） |
| GET | /schema.json/ | OpenAPI仕様（JSON） |

---

## 6. WebSocket API

### 接続

```
ws://{host}/ws/capsule/{capsule_id}/
```

### 認証
JWTトークンをクエリパラメータまたはヘッダーで送信

### メッセージ形式

#### 接続時
```json
{
  "type": "connection_established",
  "user_id": "uuid",
  "capsule_id": "uuid"
}
```

#### 位置情報更新
```json
{
  "type": "location_update",
  "latitude": 35.6762,
  "longitude": 139.6503
}
```

#### メンバー参加通知
```json
{
  "type": "member_joined",
  "user_id": "uuid",
  "member_count": 3,
  "capacity": 5
}
```

#### カプセルロック通知
```json
{
  "type": "capsule_locked",
  "capsule_id": "uuid"
}
```

---

## 7. ��証・セキュリティ

### JWT認証
- アクセストークン: JWTトークン
- リフレッシュトークン: トークン更新用
- ヘッダー形式: `Authorization: JWT {token}`

### セキュリティ機能
- ログイン試行のレート制限
- メール確認（6桁コード）
- パスワードリセット（セキュアトークン）
- ソフトデリート（メールアドレス匿名化）
- IPアドレス追跡

---

## 8. 環境設定

### 必須環境変数

```bash
# Django
DB_SECRET_KEY=           # Djangoシークレットキー

# データベース（本番）
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Redis
REDIS_HOST=

# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=
AWS_STORAGE_REGION=

# メール
EMAIL_HOST=
EMAIL_PORT=
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=

# CORS
CORS_ORIGIN_WHITELIST=
```

### オプション環境変数

```bash
# サービス情報
SERVICE_NAME=
SERVICE_COPY=
COMPANY_NAME=

# 外部API
OPENAI_API_KEY=
STRIPE_API_KEY=
STRIPE_API_SECRET=
GOOGLE_APPLICATION_CREDENTIALS_STR=
SLACK_WEBHOOK_URL=

# Twitter OAuth
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_BEARER_TOKEN=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=
```

---

## 9. 開発環境セットアップ

### 前提条件
- Python 3.10+
- Poetry
- Docker & Docker Compose

### セットアップ手順

```bash
# 1. 依存関係インストール
poetry install

# 2. 環境変数設定
cp .env.sample .env
# .envを編集

# 3. マイグレーション
python manage.py migrate

# 4. 開発サーバー起動
make run
# または
python manage.py runserver
```

### Docker開発環境

```bash
docker-compose up
```

---

## 10. 本番デプロイ

### Docker Compose構成

```yaml
services:
  - mysql:5.7      # データベース
  - redis          # メッセージブローカー
  - setup          # マイグレーション実行
  - app            # uWSGI (ポート8000)
```

### デプロイコマンド

```bash
docker-compose -f docker-compose.prd.yaml up -d
```

---

## 11. Makefileコマンド

| コマンド | 説明 |
|---------|------|
| make shell | Poetry shell起動 |
| make run | 開発サーバー起動 |
| make migrate | マイグレーション実行 |
| make migrate-make | マイグレーションファイル作成 |
| make test | テスト実行 |
| make superuser | 管理者ユーザー作成 |
| make install | 依存関係インストール |
| make add pkg=xxx | パッケージ追加 |
| make schema-gen | APIスキーマ生成 |

---

## 12. 未実装・コメントアウト機能

以下の機能はモデルは存在するが、APIとして未統合：

- **Tweet（ツイート）**: テキスト・画像投稿機能
- **Tag（タグ）**: タグ付け機能
- **Web Views**: Djangoテンプレートビュー

コメントアウトされているエンドポイント：
- メール確認フロー
- パスワードリセットエンドポイント

---

## 13. 外部サービス連携

| サービス | 用途 |
|---------|------|
| AWS S3 | ファイルストレージ |
| AWS SES | メール送信 |
| Firebase | リアルタイム機能・分析 |
| OpenAI | コンテンツ生成・分析 |
| Google Cloud Translation | 翻訳 |
| Slack | Webhook通知 |
| Stripe | 決済処理 |
| Twitter | OAuth認証 |

---

## 14. API テスト例

```bash
# ダミーユーザー作成
http post http://localhost:8000/api/register/dummy/

# ログイン
http post http://localhost:8000/api/auth/user/ \
  email=test@test.com password=testuser

# ユーザー情報取得（認証付き）
http http://localhost:8000/api/user/ \
  Authorization:"JWT $TOKEN"

# カプセル作成
http post http://localhost:8000/api/capsules/ \
  Authorization:"JWT $TOKEN" \
  title="マイカプセル" description="テスト"

# WebSocket接続テスト
wscat -c ws://localhost:8000/ws/capsule/{capsule_id}/
```
